# Copy to .env.local and fill in. Both values are safe in the client bundle —
# Supabase row-level security is what protects the tables.
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-publishable-key
