{
  "name": "jsro",
  "version": "1.0.0",
  "private": true,
  "description": "JSRO — Jatin Space & Robotics Organization. Website and server.",
  "workspaces": ["frontend", "backend", "packages/*"],
  "scripts": {
    "start": "npm start --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "test": "npm test --workspace=frontend",
    "server": "npm start --workspace=backend"
  },
  "engines": { "node": ">=20" }
}
